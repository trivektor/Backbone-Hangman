#east
  #hint 
    %span Hint: It's a country

  #word Click new game to begin
  
  #characters
    - ('A'..'Z').each do |l|
      %span.action_button.character{:char => l}= l
    %span.action_button.character{:char => "&"} &
    
  #answer